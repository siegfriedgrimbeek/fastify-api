// Import Swagger documentation
// const documentation = require('./documentation/carApi')
const router = require('../core/base/route')
let { routes } = router('car')

/**
 * Adding new route here
 */

module.exports = routes
